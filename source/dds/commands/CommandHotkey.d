module dds.commands.CommandHotkey;

// TODO: This depends on having an enum(?) for Key.
// Which becomes complicated due to unicode or IME.

/// Allows association of a command to a keyboard press.
@disable class CommandHotkey {
	this() {
		// Constructor code
	}


}

